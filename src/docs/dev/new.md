# 待更新功能描述

新增或更新后请及时打[x]，并移除距今3天（含）的已完成内容

- [x] 工具页新增「找不同辅助」
完成时间：2026-08-16
完成说明：新增 `/tool/spotdiff` 工具，上传找不同游戏截屏后，用 5 条参考线（左右模式：A左缘 / 分界(红线，=B左缘=A右缘) / B右缘 + 顶部 / 底部两条正交线；上下模式反之）精确框出 A/B 两图，按原始分辨率逐像素做差（max(|ΔR|,|ΔG|,|ΔB|)，不做降采样），自适应对比度输出清晰的黑白差异图（黑=相同、白/亮=差异，无需手动调阈值）。线可任意拖动、自动按位置排序。差异图下方提供「尝试自动对齐」（分界线附近 ±10px 内逐像素暴力比较，取界面"差异像素"数最小的位置，1/2 尺度采样加速、不做粗精修与降噪，"对齐中…"按钮反馈与分界线移动量提示）与「微调分界线」（0.2/0.5/1/2/5/10px 步长按钮或 Ctrl+方向键，分割位置保留浮点 + 平滑采样，支持亚像素对齐）用于手动/自动对齐。视图为「差异图 / 原图+标红」：原图标红仅标记差异亮度 ≥60% 的强差异像素，自动滤掉微弱 JPEG 噪点；对剩余强差异做 8-连通域聚类并画红色外接框（面积 <10px 的点状噪点不画，相交/包含的框合并成大方框），统计行显示"差异像素 · 区域 N"；支持下载全分辨率 PNG。参考线与步长持久化到 localStorage（`spotdiff-settings-v2`），并已在 `assets/pages/tool/tool.json` 的「图片」分类登记。

- [x] 主页的动态显示，应直接在每条下方显示其对应评论（显示最新3条），并可直接在主页点赞
完成时间：2026-04-22
完成说明：主页动态卡片已新增最新 3 条评论预览区，并支持在主页直接点赞；点赞数与评论数会随 Firebase 数据实时更新。

- [x] 登录管理页/admin-user，新增“显示/导出注册用户列表”功能
完成时间：2026-04-22
完成说明：已在 `/a/admin-users` 新增注册用户列表面板与“导出注册用户”功能，并补充 `/admin-user` 跳转入口兼容旧路径。

- [x] 大幅优化 `/a/terminal`，补充常见 shell 指令并接入 LLM 对话能力
完成时间：2026-04-23
完成说明：已重构 `/a/terminal` 的路径解析、文件系统与命令执行层，补充 `pwd`、`echo`、`find`、`grep`、`wc`、`head`、`tail`、`cp`、`mv`、`stat`、`env/export/unset` 等常见 shell 指令，并新增 `llm`、`llm-config`、`llm-key`、`llm-base`、`llm-model`、`llm-history` 等命令；同时让 `/a/qai` 与 `/a/terminal` 共用同一套 localStorage LLM 配置，若已在 `/a/qai` 保存 API Key / Base URL / Model，终端可直接读取后对话。

- [x] `/a/qai` 大版本更新：6 项新功能
完成时间：2026-05-09
完成说明：
  1. **对话分类**：侧栏新增分类栏，支持创建/重命名/删除分类；对话可拖拽到分类标签上移动；按分类筛选显示
  2. **移动端侧栏**：侧栏头部新增关闭按钮；点击右侧空白处收起侧栏；左滑/右滑手势
  3. **消息星标备注**：每条 AI 回复可星标并添加备注；侧栏「星标」按钮查看/管理所有星标
  4. **命令系统**：输入 `/` 触发自动补全；/new /clear /settings /persona /templates /export /summarize /translate /starred /help；Tab/方向键/Enter 操作
  5. **提示词模板库**：管理提示词模板（含系统提示词和用户前缀）；可弹窗应用或通过 `/模板名` 命令调用；比人格更灵活
  6. **AI 输出中断**：生成时显示红色停止按钮；点击中断保留已生成内容；可复制/下载/星标/重新生成

- [x] `/a/qai` 二次功能新增 x6
完成时间：2026-05-10
完成说明：
  1. **重新生成增加改写选项**：重新生成按钮改为下拉菜单，支持「重新生成」「更详细」「更简洁」「更专业」「更口语日常」五种模式
  2. **聊天气泡模式**：设置-功能新增切换开关，用户气泡在右侧、AI气泡在左侧，带圆角聊天风格
  3. **对话加密导出/导入**：导出时可设置密码（AES-GCM 加密），导入时需输入密码解密；不设密码则以明文导出
  4. **JSON 输出模式**：设置-功能新增切换，强制模型输出合法 JSON（定义 response/thinking/confidence 结构），自动验证并格式化显示
  5. **原生请求查看**：设置-功能新增切换，开启后每条 AI 回复下方显示「查看原始请求」，点击可展开本次发送的完整请求 JSON
  6. **侧栏折叠**：人格、模板、星标按钮改为可折叠展开的侧栏区块，点击标题切换展开/收起，状态持久化
