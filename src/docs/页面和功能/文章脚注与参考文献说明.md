# 文章脚注与参考文献说明

文章页支持使用 Markdown 脚注语法编写参考文献：

```markdown
正文中的引用写作 [^id]。

[^id]: 参考文献名称或链接
```

构建时会自动将正文中的 `[^id]` 渲染为数字上标，并按正文首次出现顺序编号。文末的脚注定义会被收集到“参考文献”区域；点击正文上标可跳转到对应文献，点击文献后的返回符号可回到正文引用位置。

约定：
- `id` 只用于文章源文件内的对应关系，页面显示为自动编号。
- 同一篇文章内可多次引用同一个 `id`，文末只显示一条参考文献。
- 参考文献定义建议集中放在文章末尾，每条使用独立段落。
